<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    {{#if isGradeResponse}}
    <meta name="status" content={{status}}>
    <meta name="max_points" content={{maxPoints}}>
    <meta name="points" content={{roundedPoints}}>
    <meta name="DC.Description" content={{description}}>
    <meta name="DC.Title" content={{title}}>
    {{/if}}
    {{>csrf}}
    {{>grader_meta}}
    {{>stylesheets}}
    {{>bootstrap_scripts}}
    {{>jquery}}
    <title>{{title}}</title>
</head>

<body>
    {{>navigation}}
    <div class="container">
        {{{body}}}
    </div>
</body>

</html>